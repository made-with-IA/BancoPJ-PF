<% if (locals.errors && errors.length > 0) { %>
  <div class="alert alert-error">
    <ul class="error-list">
      <% errors.forEach(function(err) { %>
        <li><%= err.message || err %></li>
      <% }) %>
    </ul>
  </div>
<% } %>
